# 2026-05-25 工作日志

## Hexo 博客侧边栏搜索框
- 在首页右侧边栏博主简介（card_author）上方添加了搜索框
- 利用 Butterfly 的 `card_top_self` 机制 + `source/_data/widget.yml` 自定义注入
- 搜索数据源：`hexo-generator-search` 生成的 `/search.xml`
- 搜索方式：侧边栏内联展示结果（非弹窗）
- 新建文件：
  - `source/_data/widget.yml` — 搜索框卡片 HTML 定义（top 组）
  - `source/js/sidebar-search.js` — 搜索逻辑（实时搜索、防抖、高亮）
  - `source/css/sidebar-search.css` — 搜索框样式（适配亮暗主题）
- 修改文件：
  - `_config.butterfly.yml` — inject.head 引入 CSS，inject.bottom 引入 JS
- 注意：widget.yml 结构必须是 `top:` + 数组，不能直接写成根级数组

## 新博文：Shannon AI 渗透测试工具
- 基于 https://github.com/KeygraphHQ/shannon 撰写中文博文
- 文件：`source/_posts/shannon-ai-pentest.md`
- 分类：AI工具箱，标签：AI/渗透测试/安全工具/开源项目/LLM/Claude

## 新博文：Crawl4AI 爬虫工具
- 文件：`source/_posts/crawl4ai-guide.md`，封面图 `source/img/crawl4ai.jpg`

## 新博文：Browser Harness vs Playwright CLI 对比
- 文件：`source/_posts/browser-harness-vs-playwright-cli.md`
- 封面图配的是 `/img/browser-automation.jpg`，需用户自行提供
- 分类：AI工具箱，标签：AI/浏览器自动化/Python/TypeScript/开源项目/LLM/Claude Code/Playwright/CDP

## 部署踩坑记录
- 项目根目录存在 Windows 保留文件名 `nul`，会导致 `hexo deploy` 失败（`fatal: unable to index file 'nul'`），已删除
- `.deploy_git` 目录被破坏后需手动 `rmdir /s /q .deploy_git` 再 `hexo deploy` 重建
- sandbox 环境中 git credential manager 无法弹出 UI，hexo deploy 需在用户终端手动执行
- 部署流程：`git push origin source` + `hexo deploy`，两者缺一不可
